# 🎓 StudentMarket — Маркетплейс учебных работ

Платформа для покупки и продажи учебных работ (практические, лабораторные, курсовые).

## 🚀 Бесплатное развёртывание в интернете

Следуйте этой инструкции, чтобы разместить сайт **бесплатно** и сделать его доступным для всех.

---

## 📋 Что мы используем (всё бесплатно!)

| Сервис | Зачем | Тариф |
|--------|-------|-------|
| **GitHub** | Хранение кода | Бесплатно |
| **Neon** | База данных PostgreSQL | Бесплатно (0.5 GB) |
| **Render** | Хостинг сервера | Бесплатно |

---

## 📝 Шаг 1: Загрузка кода на GitHub

### 1.1 Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или зарегистрируйтесь)
3. Нажмите **"+"** → **"New repository"**
4. Назовите репозиторий, например: `student-market`
5. Сделайте репозиторий **Public**
6. Нажмите **"Create repository"**

### 1.2 Загрузите файлы проекта

Откройте PowerShell в папке проекта и выполните:

```powershell
# Инициализация Git
git init

# Добавляем все файлы
git add .

# Первое сохранение
git commit -m "Initial commit"

# Добавляем GitHub как удалённый репозиторий
# Замените YOUR_USERNAME на ваш логин GitHub
git remote add origin https://github.com/YOUR_USERNAME/student-market.git

# Отправляем код на GitHub
git branch -M main
git push -u origin main
```

---

## 🗄️ Шаг 2: Создание базы данных на Neon

### 2.1 Регистрация на Neon

1. Перейдите на [neon.tech](https://neon.tech)
2. Нажмите **"Sign Up"**
3. Войдите через GitHub (рекомендуется) или через email

### 2.2 Создание проекта

1. Нажмите **"Create a project"**
2. Введите название: `StudentMarket`
3. Выберите регион (ближайший к вам, например: `aws-eu-central-1` для Европы)
4. Нажмите **"Create project"**

### 2.3 Получение строки подключения

1. После создания проекта вы увидите **Connection string**
2. Скопируйте строку вида:
   ```
   postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
   ```
3. **Сохраните её!** Она понадобится на следующем шаге.

---

## 🖥️ Шаг 3: Размещение на Render

### 3.1 Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Нажмите **"Get Started for Free"**
3. Войдите через GitHub

### 3.2 Создание сервиса

1. Нажмите **"New +"** → **"Web Service"**
2. Нажмите **"Connect a repository"**
3. Выберите ваш репозиторий `student-market`
4. Заполните настройки:

| Поле | Значение |
|------|----------|
| **Name** | `student-market` |
| **Region** | Frankfurt, Germany (или ближайший) |
| **Branch** | `main` |
| **Root Directory** | (оставьте пустым) |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `node server.js` |
| **Instance Type** | `Free` |

### 3.3 Добавление переменных окружения

Нажмите **"Advanced"** → **"Add Environment Variable"** и добавьте:

```
Name: DB_MODE
Value: postgres
```

```
Name: DATABASE_URL
Value: (ваша строка подключения от Neon из шага 2.3)
```

```
Name: NODE_ENV
Value: production
```

### 3.4 Запуск

1. Нажмите **"Create Web Service"**
2. Начнётся сборка (около 2-5 минут)
3. Когда появится зелёная галочка — сервер запущен!
4. Ваш сайт доступен по адресу: `https://student-market-xxxx.onrender.com`

---

## 🌐 Шаг 4: Бесплатный домен (опционально)

### Вариант 1: Домен от Render

Ваш сайт уже имеет бесплатный домен:
```
https://student-market-xxxx.onrender.com
```

### Вариант 2: Свой домен (бесплатно на год)

1. Зарегистрируйтесь на [freenom.com](https://freenom.com)
2. Выберите бесплатный домен (.tk, .ml, .ga, .cf, .gq)
3. В настройках домена укажите:
   - **Type:** `CNAME`
   - **Name:** `www`
   - **Value:** `student-market-xxxx.onrender.com`

4. На Render перейдите в **Settings** → **Custom Domains**
5. Добавьте ваш домен

---

## 🔧 Локальный запуск (для тестирования)

```powershell
# Установка зависимостей
npm install

# Запуск сервера
npm start
```

Откройте в браузере: `http://localhost:3000`

**Администратор по умолчанию:**
- Email: `admin@studentmarket.ru`
- Пароль: `admin123`

---

## 📁 Структура проекта

```
student-market/
├── index.html          # Главная страница
├── css/style.css       # Стили
├── js/script.js        # Клиентская логика
├── server.js           # Сервер (SQLite для локальной разработки)
├── server-pg.js        # Сервер (PostgreSQL для продакшена)
├── package.json        # Зависимости
├── render.yaml         # Конфигурация для Render
└── README.md           # Эта инструкция
```

---

## ⚠️ Важно!

### Для локальной разработки
Используется **SQLite** (файл `database.sqlite`). Все данные хранятся локально.

### Для продакшена (Render)
Используется **PostgreSQL** (Neon). Данные хранятся в облаке и не теряются.

Переключение происходит автоматически через переменную `DB_MODE`:
- `DB_MODE=sqlite` — локальный режим
- `DB_MODE=postgres` — продакшен режим

---

## 🆘 Решение проблем

### Ошибка: "DATABASE_URL не указан"

Добавьте переменную окружения `DATABASE_URL` в настройках Render с вашей строкой подключения от Neon.

### Ошибка: "Cannot find module 'pg'"

Выполните локально:
```powershell
npm install
git add .
git commit -m "Add node_modules info"
git push
```

### Сервер на Render "засыпает"

На бесплатном тарифе Render сервер "засыпает" после 15 минут бездействия. Первый запрос после простоя будет обрабатываться ~30 секунд. Это нормально.

### База данных пустая

Убедитесь, что:
1. Строка подключения `DATABASE_URL` правильная
2. Переменная `DB_MODE=postgres` установлена
3. Перезапустите сервер на Render (кнопка "Restart")

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи на Render (вкладка **Logs**)
2. Проверьте базу данных на Neon (вкладка **Tables**)
3. Убедитесь, что все переменные окружения установлены

---

## 🎉 Готово!

Ваш сайт доступен всем пользователям в интернете!

**Что дальше:**
- Поделитесь ссылкой с друзьями
- Создайте тестовые товары через админ-панель
- Пригласите первых пользователей

**Админ-панель:** `https://your-app.onrender.com` (войдите как администратор)
